{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "outDir": "../out-tsc/app",
    "types": ["jquery","bootstrap"] // 缺少这个配置则第三方引入插件使用时候会报错
  },
  "exclude": [
    "test.ts",
    "**/*.spec.ts"
  ]
}
